export interface Project {
  id: string
  title: string
  description: string
  tags: string[]
  imageUrl: string
  slug: string
}

export interface Expertise {
  id: string
  title: string
  description: string
  iconUrl: string
}
